spring:
  jpa:
    #전송 쿼리 확인
    hibernate:
      ddl-auto: update #//서버 시작이나 종료 시 DB에 저장된 데이터를 유지하도록 설정
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/cheerup
    username: root
    password: chung0916


